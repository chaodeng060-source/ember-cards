{
  "name": "ember-cards-worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Ember · 余温 的 MCP 服务端：Cloudflare Worker + Durable Object，一桌一个对象，AI 直接调工具上桌。",
  "license": "PolyForm-Noncommercial-1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "wrangler types && tsc -p tsconfig.json",
    "test": "cd .. && npm test"
  },
  "devDependencies": {
    "typescript": "~5.8.3",
    "wrangler": "^4.128.0"
  }
}
